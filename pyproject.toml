[project]
name = "tosemfdk-blog-tools"
version = "0.1.0"
description = "Utility tooling for Notion to Jekyll conversion"
requires-python = ">=3.10"
dependencies = [
  "pillow>=10.0.0",
  "python-dotenv>=1.0.0",
  "requests>=2.31.0",
]

